# Server-side rewrite target for /api/* requests.
# Local development should point to apps/api.
API_INTERNAL_URL=http://localhost:4000

# Runtime environment marker exposed only if explicitly prefixed with NEXT_PUBLIC.
NEXT_PUBLIC_APP_ENV=local